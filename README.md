# Env Requirements

-   mason:
    -   Unix:
        -   git
        -   curl / wget
        -   unzip
        -   GNU tar (tar / gtar depending on platform)
        -   gzip
    -   Windows:
        -   pwsh / powershell
        -   git
        -   GNU tar
        -   7zip / peazip / archiver / winzip / WinRAR
-   venv-selector:
    -   fd-find
-   telescope:
    -   ripgrep
    -   cmake
